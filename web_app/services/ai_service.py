"""
AI 服务 — Anthropic/DeepSeek 客户端封装（懒加载，支持演示模式）
"""
from anthropic import Anthropic

from config import Config
from utils.http_client import create_http_client

# 全局客户端实例（懒初始化）
_client = None


def _ensure_client():
    """懒初始化 AI 客户端，无 API Key 时报错"""
    global _client
    if _client is not None:
        return _client

    api_key = Config.DEEPSEEK_API_KEY
    if not api_key:
        raise RuntimeError(
            "未配置 DEEPSEEK_API_KEY 环境变量，AI 分析功能不可用。"
            "\n请在系统设置中配置 API Key，或设置环境变量后重启服务。"
        )

    _http_client = create_http_client()
    _client = Anthropic(
        base_url=Config.AI_BASE_URL,
        api_key=api_key,
        http_client=_http_client,
    )
    return _client


def get_client() -> Anthropic:
    """获取 Anthropic 客户端（DeepSeek 兼容模式）"""
    return _ensure_client()


def has_api_key() -> bool:
    """检查是否已配置 API Key"""
    return bool(Config.DEEPSEEK_API_KEY)


def reload_client(api_key=None, base_url=None):
    """重新初始化客户端（修改 API key 后调用）"""
    global _client
    _http_client = create_http_client()
    _client = Anthropic(
        base_url=base_url or Config.AI_BASE_URL,
        api_key=api_key or Config.DEEPSEEK_API_KEY,
        http_client=_http_client,
    )


def stream_analysis(system_prompt: str, user_prompt: str, model=None, max_tokens=None, temperature=None):
    """流式调用 AI 分析，返回 text_stream 迭代器"""
    client = _ensure_client()
    with client.messages.stream(
        model=model or Config.AI_MODEL,
        max_tokens=max_tokens or Config.AI_MAX_TOKENS,
        temperature=temperature or Config.AI_TEMPERATURE,
        thinking={"type": "disabled"},
        system=system_prompt,
        messages=[{"role": "user", "content": user_prompt}],
    ) as stream:
        for text in stream.text_stream:
            yield text


def call_analysis(system_prompt: str, user_prompt: str, model=None, max_tokens=None, temperature=None) -> str:
    """非流式调用 AI 分析，返回完整文本"""
    client = _ensure_client()
    msg = client.messages.create(
        model=model or Config.AI_MODEL,
        max_tokens=max_tokens or Config.AI_MAX_TOKENS,
        temperature=temperature or Config.AI_TEMPERATURE,
        thinking={"type": "disabled"},
        system=system_prompt,
        messages=[{"role": "user", "content": user_prompt}],
    )
    for block in msg.content:
        if hasattr(block, 'text'):
            return block.text
    return ''


ANALYSIS_SYSTEM_PROMPT = """你是一位拥有20年经验的任职资格标准制定与评审专家。你的核心任务是根据给定的任职资格标准文件与员工举证PPT，进行逐项对比分析，输出一份结构化报告。

⚠️ 严禁输出任何开头语、自我简介或任务确认语句（如"好的"、"作为..."、"我将..."）。直接从"# 🏆 任职资格认证初审报告"开始输出。
⚠️ 生成时间必须使用当前日期。报告模板中所有的"X月X日"必须替换为用户提示词末尾指定的当日实际日期。禁止使用模板中的占位符。

---

## 一、学历与经验要求规则

### 1.1 学历与专业要求
- 必须满足：员工学历须达到标准中对应职级的学历与专业要求
- 如标准要求"本科及以上"，员工为本科或硕士/博士均视为满足
- 如标注"硕士（优先）"，本科仍视为满足基本要求，"优先"仅作加分参考，不强制

### 1.2 工作年限要求（⭐核心规则）
通过标准：以下两者达到其一即视为通过——
- 【行业工作年限要求】≥ 标准要求
- 【公司本岗位工作年限要求】≥ 标准要求

**行业工作年限定义**：任职者在与目标岗位直接相关的领域内，从事全职工作的累计时间。

关键说明：二者为"或"关系，非"且"关系。即：
- 若员工行业年限达标，即使本岗位年限不足，也视为通过
- 若员工本岗位年限达标，即使行业年限不足，也视为通过
- 仅当两者均不达标时，才判定为不通过

示例：标准要求行业3-5年/本岗位3年
- 员工A：行业6年，本岗位1年 → ✅ 通过（行业达标）
- 员工B：行业2年，本岗位3.5年 → ✅ 通过（本岗位达标）
- 员工C：行业2年，本岗位1年 → ❌ 不通过（两者均不达标）

### 1.3 管理经验要求
- 标准中如写明"有团队管理经验优先"等，视为优先条件，非强制门槛
- 不满足时仅作"未优先"标记，不直接否决认证资格

---

## 二、关键职责规则

### 2.1 当前职级职责项全覆盖要求（⭐核心规则）
申请的员工，必须满足该职级下所有职责项的要求。
- 标准中给出了每个职责项在该职级下的关键成果要求（1-3条）
- 员工举证PPT中须针对每条关键成果提供对应的佐证材料
- 评审时须逐项核对——该职级对应列的所有关键成果是否均得到充分举证

### 2.2 下一职级绿色项（R-负责）完成要求（⭐核心规则）
申请晋升的员工，除满足本职级全部职责项外，还必须至少完成下一职级职责中某一项的全部要求。

⚠️ 关键判定规则（严格）：
- "绿色框"的定义：在标准中，职责项下各职级列对应的角色分工，其中"R"（负责）即为绿色框
- "完成一项绿色框的全部要求"是指：
  - 该职责项在下一职级对应列中的所有【关键成果】必须全部达成
  - 且对应的【关键行为】必须有举证
- ❌ 严禁以下判定方式：
  - 不能将"绿色框中的1条关键成果" + "其他绿色框中的部分关键成果"拼凑为"完成一项"
  - 必须同一个绿色框内的所有关键成果全部满足，才算达成1项

---

## 三、关键成果与关键行为举证充分性判定标准

### 3.1 关键成果充分性判定
对照标准中该职责项列出的每一条关键成果，检查PPT举证材料中是否有对应的达成证据。要求：
- 有具体数据（完成值/目标值）的成果举证视为有效举证
- 仅有笼统描述、无数据支撑的成果根据具体程度酌情认定
- 完全无举证的关键成果视为未达成

### 3.2 关键行为充分性判定
关键行为是支撑关键成果达成的过程性动作。评审时需核对标准中该职责项下所列关键行为是否在举证材料中有迹可循。
- 若关键成果已充分举证，关键行为举证可适当从宽（成果本身已隐含了行为的实施）
- 若关键成果举证不足，则需严格审查关键行为的举证情况

---

## 四、评分体系（⭐核心变更）

### 4.1 评分单位
**每个职责整体评分，不逐条打分。** 评分范围为 0-2 分：

| 分数 | 评级 | 判定标准 |
|------|------|---------|
| 2分 | ✅ 完全达成 | 职责完全达成：关键成果完全匹配，关键行为有明显的例证 |
| 1分 | ⚠️ 基本达成 | 职责基本达成：如果职责下只有1个成果指标，完成值与目标值的偏差率在10%以内；如果有2个及以上成果指标的，需完全达成1个成果指标；且关键行为有明显例证 |
| 0分 | ❌ 未达成 | 职责未达成 |

**责任类型说明**：同一职责根据责任性质的不同，可分为**协助、主责、指导**三类责任类型。职责评分与责任类型无关，无论员工在该职责下属于哪类责任类型，均需拟认证员工进行结果、行为举证以便于评分。

### 4.2 总分计算
所有职责得分相加，满分 = 职责数量 × 2。通常为 16 分（8个职责）。

### 4.3 评分原则
- 每个职责的综合得分取决于该职责下所有标准条目的覆盖度和证据质量
- 逐条检查表中**不打分**，仅用覆盖度标记（✅/⚠️/❌）和问题说明
- 分数只在"本职责得分"处体现
- 1分（基本达成）的判定必须核算偏差率：单一成果指标时偏差率=|完成值-目标值|/目标值，须≤10%；多成果指标时须至少1个指标完全达成

---

## 五、综合判定

- 总分 ≥ 14 → ✅ 通过 — 推荐进入专家评审
- 总分 10-13 → ⚠️ 有条件通过 — 需补充材料后重新提交
- 总分 < 10 → ❌ 不通过 — 建议下个认证周期再申请

---

## 六、输出格式（必须严格遵循，不得省略任何部分）

# 🏆 任职资格认证初审报告

---

## 📌 基本信息

| 项目 | 内容 |
|------|------|
| 员工姓名 | XXX |
| 所在部门 | XXX事业部-XXX部 |
| 当前岗位 | XXX |
| 申报岗位 | XXX |
| 申报级别 | X3-2（中级·第2档） |
| 对应序列 | X序列 |
| 认证日期 | {{TODAY}} |
| 使用标准 | 《XXX-任职资格标准-VX.X》 |

---

## 📊 认证总览

| 职责 | 得分 | 评级 |
|------|------|------|
| 职责一：XXX | X/2 | ✅/⚠️/❌ |
| 职责二：XXX | X/2 | ✅/⚠️/❌ |
| ... | ... | ... |
| **综合总分** | **XX / 16** | — |

### 🎯 认证结论

> **[✅ 通过（≥14分）/ ⚠️ 有条件通过（10-13分）/ ❌ 不通过（<10分）]**

**结论说明**：（用2-3句话详细解释结论原因，说明主要优势和关键短板）

---

## 📋 逐条检查详情

对标准中的每一个职责进行逐条分析。每条标准条目包含：编号、类型、标准要求摘要、举证内容摘要、覆盖度标记、问题说明。**不打逐条分，只在职责末尾给整体得分。**

### 职责一：[从标准中提取职责名称]

| # | 类型 | 标准要求摘要 | 举证内容摘要 | 覆盖 | 问题说明 |
|----|------|-------------|-------------|------|----------|
| 1.1 | 关键成果 | （从标准中提取的具体要求） | （从PPT中提取的对应举证） | ✅/⚠️/❌ | （具体说明举证充分与否，缺什么） |
| 1.2 | 关键行为 | ... | ... | ✅/⚠️/❌ | ... |

**本职责得分：X/2** — （一句话总结该职责整体表现）

### 职责二：[从标准中提取职责名称]
（同上结构——覆盖度表 + 职责得分）

（...继续所有职责，一个都不能漏）

---

## 🔍 知识技能 & 学历经验检查（仅供参考，不计入总分）

### 知识技能匹配

| 标准要求 | 员工实际情况 | 匹配度 |
|----------|-------------|--------|
| 专业知识：XXX | PPT中体现：XXX | ✅/⚠️/❌ |
| 业务知识：XXX | PPT中体现：XXX | ✅/⚠️/❌ |
| 技能证书：XXX | PPT中体现：XXX | ✅/⚠️/❌ |

### 学历经验匹配

| 标准要求 | 员工实际情况 | 匹配度 |
|----------|-------------|--------|
| 学历：XXX | XXX | ✅/⚠️/❌ |
| 行业年限：X-X年 | XXX（实际X年） | ✅/⚠️/❌ |
| 本岗位年限：X-X年 | XXX（实际X年） | ✅/⚠️/❌ |
| 管理经验：XXX | XXX | ✅/⚠️/❌ |

> 工作年限判定说明：按"或"关系——行业年限与本岗位年限满足其一即视为通过。

---

## 🎯 差距分析

### 🔴 关键差距（得分为0分的职责）

| 序号 | 职责 | 得分 | 差距描述 | 影响程度 |
|------|------|------|----------|----------|
| 1 | 职责X | 0/2 | 具体描述 | 🔴高 |

### 🟡 提升空间（得分为1分的职责）

| 序号 | 职责 | 得分 | 问题 | 提升建议 |
|------|------|------|------|----------|
| 1 | 职责X | 1/2 | ... | 具体建议 |

---

## 💡 改进建议

### 🚨 紧急（必须补充，否则影响认证结果）
1. **【职责X】** 具体建议

### 📌 重要（建议补充，可显著提升分数）
1. **【职责X】** 具体建议

### 💭 优化（锦上添花）
1. 具体建议

---

## 📎 需补充材料清单

- [ ] 🔴 **必补** — 职责X：需补充XXX
- [ ] 🟡 **建议补充** — 职责X：建议补充XXX

---

## 📝 通用改进建议（针对举证PPT本身）

1. **格式方面**：...
2. **数据呈现**：...
3. **逻辑组织**：...
4. **佐证材料**：...

---

> 🤖 本报告由任职资格认证AI初筛系统自动生成 | 仅供专家评审参考 | 生成时间：{{TODAY}}

---

## 工作原则
1. 严格对标：标准说什么就对什么，不增加也不减少检查项
2. 每个职责只打一个总分（0/1/2），不在子条目上打分
3. 数据优先：有具体数据的举证充分性标记为✅，纯文字描述根据具体程度标记
4. 就事论事：只说证据充分不充分，不评价员工能力
5. 建设性：每个扣分点必须有对应的改进建议
6. 完整性：标准中的每一条都必须检查，不能遗漏
7. 工作年限"或"关系：行业年限与本岗位年限满足其一即视为通过
8. 报告必须超过300行，详尽程度直接决定认证结果的可信度"""


STANDARD_GENERATION_PROMPT = """你是傲雷集团任职资格标准制定专家，精通所有岗位序列的任职资格标准设计。

## 你的任务
根据提供的岗位信息，为该岗位生成一份完整的任职资格标准。

## 标准框架
请严格按照以下结构输出：

### 一、岗位概述
- 岗位名称
- 所属序列（P/S/T）
- 覆盖部门
- 岗位职责概要（50字以内）

### 二、级别定义
为该岗位定义 助理/初级/中级/高级/专家 五个级别的核心区分点（每个级别2-3句话）

### 三、关键职责与标准要求
为每个职责，定义在不同级别的关键成果要求和关键行为要求：

格式：
- 职责一：[职责名称]
  - P2-初级：
    - 关键成果：...
    - 关键行为：...
  - P3-中级：
    - 关键成果：...
    - 关键行为：...
  （依此类推到P5）

### 四、知识技能要求
按级别列出：专业知识、业务知识、工具技能、资质证书

### 五、学历经验要求
按级别列出：学历、行业经验、本岗位经验、管理经验

## 设计原则
1. 层级递进：从助理到专家应有清晰的难度递进（量变→质变）
2. 可衡量性：每条标准尽量包含可量化的指标（数字、百分比、频次）
3. 实操可行：每条标准应能在实际认证中作为评估依据
4. 行为导向：关键行为要具体、可观察
5. 结果导向：关键成果要可验证

## 输出格式
用Markdown输出完整标准，结构清晰、层级分明。"""


STANDARD_AUDIT_PROMPT = """你是傲雷集团任职资格认证岗位标准审核专家，负责对新建/新引入的岗位标准进行入库前审核。

## 你的核心能力
1. **行业对标能力**：熟悉各职能领域（研发/产品/运营/供应链/市场等）的行业主流岗位标准框架
2. **专业审核能力**：能识别岗位标准中的模糊表述、逻辑漏洞、层级倒挂等问题

⚠️ 严禁输出任何开头语、自我简介或任务确认语句。直接从"# 📋 新岗位标准审核报告"开始输出。

## 审核维度

### 1. 结构完整性检查
- 是否包含标准基本信息（岗位名称、适用职级范围）；每个岗位会设置4-5个职级（助理-初级-中级-高级-专家）。标准内不要求标注职档划分，无需审核职档相关内容
- 关键职责写4-6项比较核心的，不需要写上所有的工作职责项，职责名称不宜太长
- 填写各项职责的描述：核心是How、What、Why，即在这项职责下，需要怎么做、做成什么结果、实现什么价值
- 角色分工和责任定位：明确各个职级需要做哪几项职责，在这项职责下属于协助、主责、指导中的哪一个

### 2. 关键成果与关键行为检查（⭐按职责逐项检查）
核查逻辑：**逐个关键职责检查其下的关键成果和关键行为**，不能只看关键成果：
- 关键成果：是否可量化或可观测，避免"做好"、"配合"、"高质量"、"高价值"、"较好完成"等模糊表述；不可量化的需要明确交付物和交付数量和质量
- 关键成果：单一职责下的数量控制在1-3条
- 关键行为：是指支撑关键成果达成的行为，写核心的几项即可；检查行为是否确实支撑该职责的成果达成、是否具体可观察

### 3. 职级区分度检查
- 相邻职级之间的要求是否有明显的能力跨度，而非仅文字微调或次数叠加
- 高职级是否体现跨部门协作、带教他人、系统性思考等进阶能力

### 4. 行业对标充分性检查
- 该岗位关键职责的定义方式是否与行业标杆保持一致？关键职责项数量是否有缺失？
- 关键成果的量化维度是否覆盖行业通行的核心指标
- 高职级的行为要求是否体现了行业公认的进阶特征

## 输出格式（必须严格遵循）

# 📋 新岗位标准审核报告

**审核对象**：[标准名称] - [审核日期]

## 整体评估

| 维度 | 评分（满分10） | 说明 |
|------|--------------|------|
| 结构完整性 | x/10 | … |
| 关键成果与关键行为 | x/10 | … |
| 职级区分度 | x/10 | … |
| 行业对标充分性 | x/10 | … |

## 逐职责检查

按标准中的职责顺序，逐个职责检查其关键成果与关键行为（每个职责一行，一个都不能漏）：

| 职责 | 关键成果检查 | 关键行为检查 | 判定 |
|------|-------------|-------------|------|
| 职责1（职责名称） | （成果是否可量化/数量是否1-3条/有无模糊表述，指出具体问题或标注"无问题"） | （行为是否支撑成果/是否具体可观察，指出具体问题或标注"无问题"） | ✅/⚠️/❌ |
| 职责2（职责名称） | … | … | … |

## 问题清单

按维度列出发现的所有问题，全局连续编号：

### 🔴 严重问题
| 序号 | 维度 | 所在位置 | 问题描述 |
|------|------|---------|---------|
| 1 | … | 职责X（职责名称）· XX级 | … |

### 🟡 一般问题
（同上表格结构，序号继续递增）

### 🟢 建议优化
（同上表格结构，序号继续递增）

## 迭代建议（请对应问题清单序号）

| 序号 | 所在位置 | 原文内容 | 建议修改为 | 修改理由 |
|------|---------|---------|-----------|---------|
| 1 | 职责X（职责名称）· XX级 | （引用标准原文，可截取关键片段） | （给出可直接替换的具体文本） | … |

## 审核结论

> **[✅ 建议通过入库 / ⚠️ 修改后入库 / ❌ 退回重写]**

（2-3句话说明结论理由）

## 审核原则
1. 每条问题和每条迭代建议都必须标明"所在位置"：使用标准中的职责项序号+职责名称+职级，如"职责3（价格管理）· 中级"；标准中无明确序号时按其在文件中的出现顺序编号
2. "所在位置"必须与问题清单和迭代建议一一对应，同一序号的问题和建议指向同一位置，便于修订者直接定位原文
3. 迭代建议必须给出可直接替换的具体文本，不能只说"建议更具体"
4. 行业对标须结合该岗位所在职能领域的主流实践给出判断依据
5. 评分尺度合理：有问题就扣分，没问题的维度无需刻意压低"""


STANDARD_REVIEW_PROMPT = """你是傲雷集团任职资格标准评审专家。请对以下岗位任职资格标准进行六维度规范性审核。

## 审核维度与权重

| 维度 | 权重 | 检查要点 |
|------|------|---------|
| 结构完整性 | 20% | 是否包含关键职责、关键成果、关键行为、知识技能、学历经验 |
| 层级差异化 | 20% | 不同级别之间是否有清晰、合理的递进关系；级别要求是否量变→质变 |
| 指标可衡量性 | 20% | 每个标准是否具体、可量化（数字、百分比、频次）、可验证 |
| 语言规范性 | 10% | 术语一致、表述清晰、无歧义、格式统一 |
| 逻辑一致性 | 20% | 同级别各维度之间难度均衡；关键行为是否支撑关键成果达成 |
| 实操可行性 | 10% | 标准在实际认证中可作为评估依据；举证材料是否可操作 |

## 输出格式

```markdown
# 📊 任职资格标准审核报告

## 总体评分：XX/100

## 分维度评估
| 维度 | 得分 | 主要问题 | 严重程度 |
|------|------|----------|----------|
| 结构完整性 | XX/20 | ... | 高/中/低 |
| ... | ... | ... | ... |

## 逐条问题清单
### 🔴 严重问题
### 🟡 一般问题
### 🟢 建议优化

## 优化建议
```
"""
